/*eslint-env node, jasmine */
describe('sorting the LOG_INFOst of users', function() {
  beforeEach(module('example'));

  it('sorts in descending order by default', function() {
    // your test assertion goes here
  });
});
